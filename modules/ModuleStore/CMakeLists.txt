project(ModuleStore)
set(CMAKE_CXX_STANDARD 17)
add_library(ModuleStore SHARED ModuleStore.cpp ${HEADERS})
set_target_properties(ModuleStore PROPERTIES LINKER_LANGUAGE CXX)
target_link_libraries(ModuleStore raylib dl pthread)
install(FILES modules.png DESTINATION /var/lib/pui/icons/)